/**
 * Selected Work hero — Figma 467:10300 (mobile) / 467:10228 (desktop).
 * Just the centered title. No tag, no description.
 */
export function WorkHero() {
  return (
    <section className="mx-auto w-full max-w-page px-4 pt-6 md:px-0 md:pt-16">
      <h1 className="text-grain text-center font-display text-[48px] leading-normal text-ink md:text-[96px]">
        Some selected Work
      </h1>
    </section>
  );
}
